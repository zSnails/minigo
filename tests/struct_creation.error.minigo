// The following code MUST result in an error being shown
package main;

type Person struct {
    Name string;
    Age int;
};

func NewPerson(name string, age int) Person {
    return &Person{
        Name: "Aaron",
        Age: 20,
    };
};
