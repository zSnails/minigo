// This code should not produce an error when ran.
package main;

type Person struct {
    Name string;
    Age int;
    YearBorn int;
};

type Animal struct {
    Hungry bool;
    Health int;
};

// In MiniGo there is no struct embedding.
type Dog struct {
    Animal Animal;
};
